import { HeroStyles, HeroTitle } from './styles'

const Hero = () => (
  <HeroStyles>
    <div className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </div>
  </HeroStyles>
)

export default Hero
